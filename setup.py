#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# vim: set ts=4 sw=4 et ai:

from setuptools import setup, find_packages
import os
VERSION = '0.1'

setup(
    name='lsh',
    version = VERSION,
    description = 'lsh',
    url = 'https://github.com/schwa-lab/lsh',
    packages = ['lsh'],
    install_requires = [
    ],
)
